services:

    nextjs-app:
       container_name: nextjs-app
       build:
          context: .
          dockerfile: Dockerfile
       ports:
          - "3000:3000"
       command:
          "npm run start -- --host" #for production environment 
          
#-- --host (allows to access this application from other devices
# in same network)
